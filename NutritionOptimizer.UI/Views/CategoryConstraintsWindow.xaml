<Window x:Class="NutritionOptimizer.UI.Views.CategoryConstraintsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="최적화 설정"
        Height="550"
        Width="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 제목 -->
        <TextBlock Grid.Row="0"
                   Text="최적화 설정"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0 0 0 16" />

        <!-- 카테고리별 제약 설정 -->
        <GroupBox Grid.Row="1" Header="카테고리별 제약 설정" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="카테고리별로 최소 선택 개수와 각 음식당 최대 선택 횟수를 설정할 수 있습니다."
                           FontSize="11"
                           Foreground="Gray"
                           Margin="0 0 0 8"
                           TextWrapping="Wrap" />

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding MainViewModel.CategoryConstraints}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="True"
                          CanUserDeleteRows="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="카테고리 (예: 밥, 반찬, 간식)"
                                            Binding="{Binding Category, UpdateSourceTrigger=PropertyChanged}"
                                            Width="*" />
                        <DataGridTextColumn Header="최소 개수"
                                            Binding="{Binding MinCount, UpdateSourceTrigger=PropertyChanged}"
                                            Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="최대 횟수 (배수)"
                                            Binding="{Binding MaxCountPerFood, UpdateSourceTrigger=PropertyChanged}"
                                            Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="2" Margin="0 12 0 0">
                    <TextBlock Text="설명:"
                               FontSize="11"
                               FontWeight="Bold"
                               Foreground="Gray"
                               Margin="0 0 0 4" />
                    <TextBlock FontSize="11"
                               Foreground="Gray"
                               TextWrapping="Wrap">
                        • 최소 개수: 해당 카테고리에서 최소 몇 개 이상의 음식을 선택해야 하는지<LineBreak/>
                        • 최대 횟수: 해당 카테고리의 각 음식이 최대 몇 번(배수)까지 선택될 수 있는지<LineBreak/>
                        • 예: "밥" 카테고리에서 최대 횟수가 3이면, 현미밥은 최대 3배수까지만 선택됨
                    </TextBlock>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- 하단 버튼들 -->
        <Grid Grid.Row="2" Margin="0 16 0 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="➕ 카테고리 추가"
                    Click="AddCategory_Click"
                    Width="120"
                    Height="32" />

            <Button Grid.Column="2"
                    Content="🗑️ 초기화"
                    Click="ClearCategories_Click"
                    Width="100"
                    Height="32"
                    Margin="0 0 8 0" />

            <Button Grid.Column="3"
                    Content="✔️ 확인"
                    Click="OK_Click"
                    Width="100"
                    Height="32"
                    FontWeight="Bold" />
        </Grid>
    </Grid>
</Window>
