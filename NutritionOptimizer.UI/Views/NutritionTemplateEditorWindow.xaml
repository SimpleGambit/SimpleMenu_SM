<Window x:Class="NutritionOptimizer.UI.Views.NutritionTemplateEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="템플릿 편집 (100g 기준)"
        Height="700"
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 제목 -->
        <TextBlock Grid.Row="0"
                   Text="영양성분 템플릿 편집 (100g 기준)"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0 0 0 16" />

        <!-- 입력 폼 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- ID -->
                <TextBlock Text="ID" FontWeight="SemiBold" />
                <TextBox Text="{Binding Id}"
                         IsReadOnly="True"
                         Background="#F5F5F5"
                         Margin="0 4 0 12" />

                <!-- 이름 -->
                <TextBlock Text="이름 * (예: 현미, 닭가슴살)" FontWeight="SemiBold" />
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0 4 0 12" />

                <!-- 카테고리 -->
                <TextBlock Text="카테고리 * (예: 곡물, 단백질, 채소)" FontWeight="SemiBold" />
                <TextBox Text="{Binding Category, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0 4 0 12" />

                <!-- 주요 영양소 -->
                <Separator Margin="0 12 0 16" />
                <TextBlock Text="주요 영양소 (100g 기준)" FontWeight="Bold" FontSize="15" Margin="0 0 0 12" />

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- 칼로리 -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="칼로리(kcal)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Kcal}" Margin="0 0 0 8" />

                    <!-- 단백질 -->
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="단백질(g)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ProteinG}" Margin="0 0 0 8" />

                    <!-- 지방 -->
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="지방(g)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding FatG}" Margin="0 0 0 8" />

                    <!-- 포화지방 -->
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="포화지방(g)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SaturatedFatG}" Margin="0 0 0 8" />

                    <!-- 트랜스지방 -->
                    <TextBlock Grid.Row="4" Grid.Column="0" Text="트랜스지방(g)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding TransFatG}" Margin="0 0 0 8" />

                    <!-- 탄수화물 -->
                    <TextBlock Grid.Row="5" Grid.Column="0" Text="탄수화물(g)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding CarbsG}" Margin="0 0 0 8" />

                    <!-- 식이섬유 -->
                    <TextBlock Grid.Row="6" Grid.Column="0" Text="식이섬유(g)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding FiberG}" Margin="0 0 0 8" />
                </Grid>

                <Grid Margin="0 0 0 12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- 당류 -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="당류(g)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SugarG}" Margin="0 0 0 8" />

                    <!-- 나트륨 -->
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="나트륨(mg)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SodiumMg}" Margin="0 0 0 8" />

                    <!-- 콜레스테롤 -->
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="콜레스테롤(mg)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding CholesterolMg}" Margin="0 0 0 8" />
                </Grid>

                <!-- 무기질 -->
                <Separator Margin="0 12 0 16" />
                <TextBlock Text="무기질 (100g 기준)" FontWeight="Bold" FontSize="15" Margin="0 0 0 12" />

                <Grid Margin="0 0 0 12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="칼슘(mg)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding CalciumMg}" Margin="0 0 0 8" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="철분(mg)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding IronMg}" Margin="0 0 0 8" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="마그네슘(mg)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding MagnesiumMg}" Margin="0 0 0 8" />

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="인(mg)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding PhosphorusMg}" Margin="0 0 0 8" />

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="칼륨(mg)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding PotassiumMg}" Margin="0 0 0 8" />

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="아연(mg)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding ZincMg}" Margin="0 0 0 8" />
                </Grid>

                <!-- 비타민 -->
                <Separator Margin="0 12 0 16" />
                <TextBlock Text="비타민 (100g 기준)" FontWeight="Bold" FontSize="15" Margin="0 0 0 12" />

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="비타민 A(μg)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding VitaminAUg}" Margin="0 0 0 8" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="비타민 C(mg)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding VitaminCMg}" Margin="0 0 0 8" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="비타민 D(μg)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding VitaminDUg}" Margin="0 0 0 8" />

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="비타민 E(mg)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding VitaminEMg}" Margin="0 0 0 8" />

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="비타민 B1(mg)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding VitaminB1Mg}" Margin="0 0 0 8" />

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="비타민 B2(mg)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding VitaminB2Mg}" Margin="0 0 0 8" />

                    <TextBlock Grid.Row="6" Grid.Column="0" Text="비타민 B3(mg)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding VitaminB3Mg}" Margin="0 0 0 8" />

                    <TextBlock Grid.Row="7" Grid.Column="0" Text="비타민 B6(mg)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding VitaminB6Mg}" Margin="0 0 0 8" />

                    <TextBlock Grid.Row="8" Grid.Column="0" Text="비타민 B9(μg)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding VitaminB9Ug}" Margin="0 0 0 8" />

                    <TextBlock Grid.Row="9" Grid.Column="0" Text="비타민 B12(μg)" FontWeight="SemiBold" Margin="0 0 8 4" />
                    <TextBox Grid.Row="9" Grid.Column="1" Text="{Binding VitaminB12Ug}" Margin="0 0 0 8" />
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <!-- 버튼 -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0 16 0 0">
            <Button Content="저장"
                    Width="80"
                    Height="32"
                    Margin="0 0 8 0"
                    Click="SaveButton_Click" />
            <Button Content="취소"
                    Width="80"
                    Height="32"
                    Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
