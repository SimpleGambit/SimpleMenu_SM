<Window x:Class="NutritionOptimizer.UI.Views.OptimizationResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="최적화 결과"
        Height="700"
        Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 제목 -->
        <TextBlock Grid.Row="0"
                   Text="{Binding PicksCountText}"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0 0 0 16" />

        <!-- 선택된 음식 -->
        <GroupBox Grid.Row="1" Header="선택된 음식" Margin="0 0 0 12">
            <DataGrid ItemsSource="{Binding Picks}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      AlternatingRowBackground="#F9F9F9">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="음식" Binding="{Binding FoodName}" Width="*" />
                    <DataGridTextColumn Header="양(×100g)" Binding="{Binding Amount100g, StringFormat={}{0:N2}}" Width="100" />
                    <DataGridTextColumn Header="비용(₩)" Binding="{Binding Cost, StringFormat={}{0:N0}}" Width="100" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- 영양소 달성 -->
        <GroupBox Grid.Row="2" Header="영양소 달성" Margin="0 0 0 12">
            <DataGrid ItemsSource="{Binding NutrientSummary}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      AlternatingRowBackground="#F9F9F9">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="영양소" Binding="{Binding Key, Converter={StaticResource NutrientKeyConverter}}" Width="*" />
                    <DataGridTextColumn Header="달성" Binding="{Binding Actual, StringFormat={}{0:N1}}" Width="100" />
                    <DataGridTextColumn Header="최소" Binding="{Binding Min, StringFormat={}{0:N0}}" Width="80" />
                    <DataGridTextColumn Header="최대" Binding="{Binding Max, StringFormat={}{0:N0}}" Width="80" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- 총 비용 및 닫기 버튼 -->
        <Grid Grid.Row="3">
            <TextBlock Text="{Binding TotalCost, StringFormat=총 비용: ₩{0:N0}}" 
                       FontSize="18" 
                       FontWeight="Bold"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Center" />
            
            <Button Content="닫기"
                    Width="100"
                    Height="35"
                    HorizontalAlignment="Right"
                    Click="CloseButton_Click" />
        </Grid>
    </Grid>
</Window>
